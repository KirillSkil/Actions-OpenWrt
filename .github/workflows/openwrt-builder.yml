name: openwrt-builder

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Init dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential clang flex bison g++ gawk gcc-multilib gettext git \
            libncurses5-dev libssl-dev python3-distutils rsync unzip zlib1g-dev \
            file wget

      - name: Clone OpenWrt source
        run: |
          rm -rf openwrt
          git clone --depth 1 "https://github.com/openwrt/openwrt.git" -b openwrt-23.05 openwrt

      - name: Update & install feeds
        run: |
          cd openwrt
          ./scripts/feeds update -a
          ./scripts/feeds install -a

      - name: Add Podkop + minimal config + RU defaults
        run: |
          set -e
          cd openwrt

          # Podkop integration (package-only; no extra stuff added here)
          rm -rf package/podkop
          git clone --depth 1 "https://github.com/itdoginfo/podkop.git" package/podkop

          mkdir -p files/etc/uci-defaults

          cat > files/etc/uci-defaults/99-custom-settings <<'SH'
uci set luci.main.lang='ru'
uci set podkop.config.enabled='1'
uci set wireless.radio0.disabled='0'
uci set wireless.default_radio0.ssid='Ezel2'
uci set wireless.default_radio0.encryption='psk2'
uci set wireless.default_radio0.key='123456780'
uci commit

exit 0
SH

          # Start from a clean config for our target
          make distclean >/dev/null 2>&1 || true

          # Select target
          cp -f /dev/null .config
          echo "CONFIG_TARGET_ramips=y" >> .config
          echo "CONFIG_TARGET_ramips_mt7621=y" >> .config
          echo "CONFIG_TARGET_ramips_mt7621_DEVICE_xiaomi_mi-router-4a-gigabit=y" >> .config

          # Rootfs initramfs
          echo "CONFIG_TARGET_ROOTFS_INITRAMFS=y" >> .config

          # Keep only required LuCI language + app
          echo "CONFIG_PACKAGE_luci=y" >> .config
          echo "CONFIG_PACKAGE_luci-base=y" >> .config
          echo "CONFIG_PACKAGE_luci-i18n-russian=y" >> .config

          # Podkop daemon + LuCI app
          echo "CONFIG_PACKAGE_podkop=y" >> .config
          echo "CONFIG_PACKAGE_luci-app-podkop=y" >> .config

          # Ensure defaults are included
          # (OpenWrt includes /etc/uci-defaults automatically via default init scripts)
          make defconfig

      - name: Download sources
        run: |
          cd openwrt
          make download -j8

      - name: Compile (sysupgrade firmware only for ramips/mt7621)
        run: |
          cd openwrt
          make -j"$(nproc)" || make -j1 V=s

      - name: Show artifacts
        run: |
          set -e
          ls -la openwrt/bin/targets/ramips/mt7621/ || true
          echo "---- only sysupgrade ----"
          ls -la openwrt/bin/targets/ramips/mt7621/*sysupgrade*.bin || true

      - name: Upload sysupgrade only
        uses: actions/upload-artifact@v4
        with:
          name: OpenWrt_Sysupgrade
          path: openwrt/bin/targets/ramips/mt7621/*sysupgrade*.bin
